interface:
  display_name: "WeChat Sticker Pack"
  short_description: "Create integrated-text IP sticker packs and extension assets"
  default_prompt: "Use $wechat-sticker-pack to create a confirmed die-cut Chinese WeChat sticker style from IP references and keywords, with model-generated text inside each sticker plus covers, headers, icons, reward art, and a platform-check manifest."
