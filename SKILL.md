---
name: wechat-sticker-maker
description: Use when creating or revising WeChat sticker packs from IP reference images, including character exploration, three-view turnarounds, keyword-based expression stickers, reward or tipping art, covers, headers, and related sticker assets.
---

# 微信表情包制作

## Overview

将一个或多个 IP 形象稳定地转化为可批量生产的微信表情包资产。核心原则是先锁定角色身份，再生成单张表情；每张图独立验证，不因批量而牺牲角色一致性、文案准确性或安全性。

**REQUIRED SUB-SKILL:** 使用 `imagegen` 生成或编辑位图资产；遵循其输入图片角色标注、非破坏性保存、逐资产调用和输出检查规则。

## 已确认的主视觉风格

默认采用 [references/confirmed-sticker-style.md](references/confirmed-sticker-style.md)。核心是“一次生成完整贴纸”：厚白色不规则切边、浅灰外部背景、夸张手绘展示字、黑红情绪色、角色与道具形成完整场景，中文主文案直接由模型画进图里。

不要退回“空图＋本机叠字”、预留文字框、白色标题卡或社交媒体封面式排版。批量前先做一张风格对齐测试，用户确认后再批量生成。

## 工作流总览

1. 读取参考图，提取角色不变量和用户目标。
2. 若 IP 尚未稳定，先做身份探索；若缺少可靠角度信息，先做三视图。
3. 先从当前圈内热梗中寻找原始表情/图片，提取原文关键词和视觉锚点；用户确认候选后，再为每个关键词确定角色数量、情绪、动作、场景、道具和文案规则。
4. 每个独立表情使用一个独立生成调用；不要用一个泛化提示词替代逐张设计。
5. 生成封面、头图、打赏图等延展物料时复用已确认的角色锚点和色彩关系。
6. 对每张图做对抗性验收，修正失败项后再交付，并列出仍未满足的约束。

## 1. 识别 IP 与约束

把输入图片分成三类，不要混淆用途：

| 角色 | 用途 |
|---|---|
| 身份参考 | 锁定脸型、毛发/羽毛、颜色、服装、配饰、轮廓和比例 |
| 场景/关系参考 | 锁定两个 IP 如何相处、空间关系和画面气质 |
| 情绪/姿态参考 | 只借鉴表情、动作或构图；禁止复制真人、猴子、品牌或原图背景 |

先写一份简短的身份不变量，至少包含：

- 角色名称、物种/类型和性格职责；
- 主色、辅助色和线稿/阴影风格；
- 头部、眼睛、嘴、耳朵/角、毛发/羽毛等识别点；
- 服装、眼镜、围巾、手机等固定配件；
- 明确禁止出现的角色、道具、文字或场景。

如果无法仅凭参考图判断关键身份，先问一个最影响结果的问题；不要连续追问十个细节。其余内容采用明确默认值，并在交付时说明假设。

## 2. IP 探索与三视图

### IP 形象探索

- 用户只有模糊概念时，先生成少量方向对比，而不是直接批量做表情。
- 每个方向只改变一个变量：轮廓、材质、配色或表情语言；不要同时改变多个变量。
- 用户确认方向后，提炼为可复用的身份锚点，供后续所有调用引用。

### 三视图

- 在生成表情包前，优先生成正面、侧面、背面的全身三视图，作为后续身份参考。
- 三视图必须显示同一比例、同一服装/配饰和一致的轮廓；背面不应出现正面五官。
- 若三视图出现角度、服装或配饰冲突，先修正三视图，不要带着不稳定锚点继续批量生产。
- 默认输出一张双角色总版和每个 IP 一张独立三视图；用户另有要求时调整。

## 3. 关键词收集与表情设计

关键词不是主题词清单，也不是模型根据主题或行业场景自由脑补的句子。默认先研究当前圈内高传播的原梗，再做 IP 化转译。详细流程见 [references/keyword-research.md](references/keyword-research.md)。

每个关键词在进入生图前都必须有“原文 + 来源证据 + 原梗图/截图 + 视觉锚点 + 用户确认”。如果只有一句抽象文案、找不到对应原图，或不读文字无法视觉化，先标记为待验证或淘汰，不直接生成。

收集关键词时，至少确认：

- 关键词是否从原梗直接提取并原文放入图片；
- 原梗来源的平台、链接、检索日期和参考图是否已记录；
- 原梗最关键的动作、局部特写、景别、构图或道具是什么；
- 每张图是一只 IP、两只 IP，还是由模型自行判断；
- 是否有必须复刻的情绪/动作参考图；
- 画布比例、背景、是否需要透明、是否需要道具；
- 只有用户明确要求时才允许无文案版本；这不是默认生产分支，也不主动建议空图或预留文字框。

如果用户没有规定角色数量，按语义分配，而不是强行双人同框：

- 单人情绪、脸部特写、单个动作：优先单 IP；
- 需要安慰、争论、共同庆祝或共同研究：才使用双 IP；
- 同一批次中主动改变角色数量，避免所有图片变成同一构图模板。

每张表情都要写一个短的画面简报：

- 主体角色和性格位置；
- 动作、脸部表情和情绪强度；
- 与关键词直接相关的视觉证据；
- 原梗图中必须保留的视觉锚点；局部表情成立时保留原来的焦点和景别；
- 构图和背景；
- 文案原文；只有用户明确要求时才记录无文案例外；
- 原梗来源与用户确认状态；
- 必须避免的角色、道具、文字和危险行为。

具体提示词模板见 [references/prompt-templates.md](references/prompt-templates.md)。

## 4. 生成规则

- 使用已确认的三视图/身份锚点作为 `reference image`；情绪图片只能标为 `emotion/pose reference`。
- 每个关键词必须带对应的原梗图作为 `emotion/pose/composition reference`；没有来源图时，不让模型凭关键词自由发明画面。
- 每个独立资产使用一个独立的图像生成调用；不同关键词不要用 `n` 代替独立提示词。
- 中文文案必须放入 `Text (verbatim)`，写明准确换行，并明确“文案直接融入贴纸构图，不是单独文字框”。除非用户明确要求，不生成空图或后期叠字版本。
- 风格参考图只用于线稿、切边、色彩、字形和构图密度；禁止复制其中的标题、品牌、logo、水印或随机小字。
- 不要把当前项目已有生成图误当作最终正确参考；优先引用用户原始 IP 图和已经验收的三视图。
- 不覆盖已有资产；使用版本号或描述性后缀，例如 `v2`、`close-up`、`single`。
- 用户明确淘汰某种路线或要求“删除/忘掉”旧稿时，立即把它从活动参考图、manifest 和后续提示词中移除；若历史文件仍保留，必须标记为废弃且禁止继续引用。
- 生成完成后把最终资产复制到当前项目的工作区，不能只留在默认生成目录。

## 5. 微信表情开放平台上架模式

这不是“聊天自用表情”的规格。需要上架时，先读取 [references/open-platform-specs.md](references/open-platform-specs.md)，再按以下顺序执行：

1. 先确认提交类型：表情专辑、单品，静态或动态；不要默认把一套图直接当作专辑提交。
2. 先按平台物料清单收集数量、尺寸、格式和体积；当前平台上传页的校验结果优先于本 skill 或网上旧教程。
3. 生成原图后，再做裁切、透明背景、静态缩略图、压缩和命名；不要让 imagegen 直接承担最终上架文件的像素与体积合规。
4. 将“设计稿”和“上传稿”分开保存，并在 `manifest.md` 记录平台检查日期、检查入口、实际尺寸、格式、体积和结果。

如果当前上传页面打不开或没有显示限制，不得声称“符合官方规范”；可以按参考文件中的保守基准继续做视觉稿，但必须标记为“待平台复核”。

## 6. 延展物料

### 打赏图

先确认是“赞赏引导图”还是“赞赏感谢图”，再按平台基准尺寸制作。需要二维码、金额或文案时先划出安全区；没有文案要求时，不擅自添加中文，也不制造预留标题框，采用自然构图完成画面。打赏图不是主表情，不能沿用 240×240 的画布。

延展物料也要复用已确认的 IP 身份、线稿和黑红情绪关系，但不强行套用贴纸切边；根据物料用途决定是否透明、是否需要文字和是否需要安全区。禁止把主表情直接拉伸成封面、横幅或图标。

### 封面图/头图

上架模式下，详情页横幅、封面图和聊天面板图标分别制作，不能互相拉伸替代。尺寸、格式和体积读取 [references/open-platform-specs.md](references/open-platform-specs.md)；若未知，先确认文案或采用自然构图，不制造预留标题框。只有二维码、金额或平台 UI 等硬性信息才需要安全区。必须锁定角色数量、层级和视线方向，避免背景道具抢走 IP 主体。

### 其他相关图片

把“图标、贴纸封面、宣传海报、合集页、背景图”当成不同资产处理，分别指定比例、信息层级和文字规则；不要用一个表情包提示词直接拉伸成所有物料。

## 7. 对抗性验收

把每张图当作可能被用户否定的交付稿，至少检查：

1. **身份攻击**：是否出现错误物种、错误服装、错误配饰或未请求的第二个 IP？
2. **语义攻击**：不读文字时，动作和表情是否仍能表达关键词？
3. **文案攻击**：文字是否逐字准确、完整、可读？有没有乱码、漏字或模型擅自加字？
4. **构图攻击**：主体是否被裁切、遮挡、缩得太小或被背景道具抢走？
5. **一致性攻击**：同批次的线稿、比例、材质和颜色是否仍属于同一套 IP？
6. **数量攻击**：用户要求单人时是否只有一个 IP；用户要求双人时是否两者都出现且关系成立？
7. **安全攻击**：是否出现危险动作、模仿性自伤、血腥或其他不必要的高风险画面？不要绕过安全拦截；改成安全、非行动性的情绪表达，并重新检查文案与语义。
8. **交付攻击**：文件是否真的保存到工作区，命名是否能对应关键词，旧版和新版是否可区分？
9. **平台攻击**：是否把设计稿当上传稿？主图数量、缩略图配对、尺寸、格式、体积、透明背景和命名是否逐项通过当前上传页？
10. **规格漂移攻击**：结论是否只来自旧教程或第三方检测器？若没有当前平台证据，是否明确标记为待复核？
11. **风格回退攻击**：是否出现预留文字框、白色标题卡、空图后期叠字或社交媒体封面式排版？若出现，视为不通过。
12. **来源攻击**：是否能从 manifest 回溯到原梗来源、原文和视觉锚点？如果只能解释“这个词大概是什么意思”，没有原图证据，视为不通过。

发现失败项时，只改一个变量后再生成；不要同时改角色、构图、文字和风格。无法修正时，明确列出缺口，不要声称“已完成”。

详细验收清单见 [references/qa-checklist.md](references/qa-checklist.md)。

## 8. 交付约定

默认按以下目录组织当前项目产物：

```text
<pack-name>/
├── 00-character/
├── 01-stickers/
├── 02-extensions/
└── manifest.md
```

`manifest.md` 至少记录：资产名、关键词原文、原梗来源平台/链接/检索日期、视觉锚点、用户确认状态、角色数量、是否有文案、参考图角色、版本、平台物料类型、实际尺寸/格式/体积、平台检查日期、验收结果和已知缺口。最终回复列出每个最终文件的绝对路径，并区分“通过验收”“部分通过”“待用户决定”。
